<%= render 'home/menu' %>
<div id="aprovar_entidade">
<%= @entidade.aprovado_ent ? false : (link_to "Aprovar", { :action => "aprovado", :id => @entidade}, :class => "botao_verde" ) %>
</div>
<p id="notice"><%= notice %></p>

<%= render 'show' %>
<br /><br />
<h1>Histórico</h1>
<%= render @entidade.historicos %>

<h3>Novo Histórico</h3>
<%= render "historicos/forment" %>

<br />
