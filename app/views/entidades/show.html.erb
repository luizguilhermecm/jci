<%= render 'senhorjcis/menu' %>
<div id="corpo">
<div id="aprovar_entidade">
<% if  @entidade.aprovado_ent %>
		<p style="margin-top:35px; font-size:25px">Aprovada</p>
<%	else 
		
	(link_to "Aprovar", { :action => "aprovado", :id => @entidade}, :class => "botao_verde" ) 
	end
		%>
</div>
<div style="clear: both"></div>
<div id="aprovar_entidade" style="margin:10px 0;">
<p style="margin:20px 0 0 0">Buscar por entidade compatível com este perfil</p>
<%= button_to "Buscar", {:controller => "voluntario_pessoa_fisicas", :action => "cruzar_pf_ent" }, :method => "get", :id => @entidade.id, :class => "botao_verde"  %>
</div>
<p id="notice"><%= notice %></p>

<%= render 'show' %>

<h1>Histórico</h1>
<h3>Novo Histórico</h3>
<%= render "historicos/forment" %>
<br />
<%= render @entidade.historicos %>
<br />

</div>
