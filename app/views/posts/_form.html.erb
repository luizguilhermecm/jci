<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data_not %><br />
    <%= f.date_select :data_not %>
  </div>
  <div class="field">
    <%= f.label :data_update_not %><br />
    <%= f.date_select :data_update_not %>
  </div>
  <div class="field">
    <%= f.label :titulo_not %><br />
    <%= f.text_field :titulo_not %>
  </div>
  <div class="field">
    <%= f.label :descricao_not %><br />
    <%= f.text_area :descricao_not %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
