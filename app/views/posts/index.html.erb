<h1>Listing posts</h1>

<table>
  <tr>
    <th>Data not</th>
    <th>Data update not</th>
    <th>Titulo not</th>
    <th>Descricao not</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.data_not %></td>
    <td><%= post.data_update_not %></td>
    <td><%= post.titulo_not %></td>
    <td><%= post.descricao_not %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
