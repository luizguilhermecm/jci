<%= form_for([@voluntario_pessoa_juridica, @voluntario_pessoa_juridica.historicos.build]) do |f| %>
  <div class="field">
    <%= f.label :descricao_hist, "Descricão" %>
    <%= f.text_field :descricao_hist %>
  </div>
  <div class="actions">
  	<!-- NAO ALTERAR NOME DO BOTAO, SENDO USADO NO CONTROLLER -->
    <%= f.submit 'Historico PJ', :class => "botao_verde" %>
  </div>
<% end %>
