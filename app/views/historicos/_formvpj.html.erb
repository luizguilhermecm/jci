<%= form_for([@voluntario_pessoa_juridica, @voluntario_pessoa_juridica.historicos.build]) do |f| %>
  <div class="field">
    <%= f.label :descricao_hist %><br />
    <%= f.text_field :descricao_hist %>
  </div>
  <div class="actions">
    <%= f.submit 'vpj'%>
  </div>
<% end %>
